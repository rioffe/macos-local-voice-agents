.DS_Store
.env
.venv
.vscode
.next
node_modules
venv
__pycache__
.env.local
.env.development.local
.env.test.local
.env.production.local

# PDF build artifacts (reproducible via `make`)
docs/*.pdf

# mermaid-filter writes a stray log to the CWD on each run
mermaid-filter.err
*/mermaid-filter.err